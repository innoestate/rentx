implémentation des tests front.

On veut partir sur le postulat de tester la création d'un owner.

-> le test doit vérifier que, quand on clique sur créer owner, la popup s'affiche bien.
-> ensuite que si l'utilisateur rempli les champs correctement et valide, le store le service http est bien appelé
-> que le store est bien mis à jour
-> qu'on voit bien que le owner est mis à jour dans l'estate visible sur la page

On veut vérrifier la création d'un estate

-> l'utilisateur voit bien la popup
-> il remplit les champs et la popup se ferme
-> le service est appelé et le store mis à jour
-> l'estate est ajouté et visible dans page.
